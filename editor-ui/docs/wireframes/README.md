# Wireframes

Static mocks live in Storybook / dev routes during Phase 0:

| Screen | Route / story |
|--------|---------------|
| Login | `/login` |
| Posts list | `/posts` |
| Post editor (body chrome) | `/posts/:id` — `PostBodyEditorMock` |
| Publish wizard | `/publish` |
| Config | `/config/*` |

The post editor mock includes: sticky toolbar, stats strip, emoji palette, 700px canvas with blog typography.
